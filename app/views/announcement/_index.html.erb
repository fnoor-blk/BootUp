<%if logged_in?(:admin)%>
	<%=link_to 'Create Announcement', new_announcement_path, class: "btn btn-primary", remote: true%>
<%end%>
<%= paginate @announcements, :remote => true %>
<% @announcements.each do |announcement|%>
<%=link_to announcement.title,announcement_path(announcement),class:"dropdown-item",remote:true%>
<%end%>
<%= paginate @announcements, :remote => true %>